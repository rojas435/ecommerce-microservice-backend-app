apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecommerce-microservices.fullname" . }}-test-connection"
  labels:
    {{- include "ecommerce-microservices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecommerce-microservices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
