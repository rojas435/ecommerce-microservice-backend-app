apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ecommerce-stage
  labels:
    app: order-service
    environment: stage
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
        environment: stage
    spec:
      imagePullSecrets:
      - name: dockerhub-secret
      containers:
      - name: order-service
        image: rojas43529/order-service:stage-latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8300
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "stage"
        - name: SPRING_CLOUD_DISCOVERY_ENABLED
          value: "false"
        envFrom:
        - configMapRef:
            name: microservices-config
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8300
          initialDelaySeconds: 45
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8300
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce-stage
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
  - port: 8300
    targetPort: 8300
    name: http
  type: ClusterIP
