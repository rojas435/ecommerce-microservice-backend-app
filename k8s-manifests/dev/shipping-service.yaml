apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: ecommerce-dev
  labels:
    app: shipping-service
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
        environment: dev
    spec:
      imagePullSecrets:
      - name: dockerhub-secret
      containers:
      - name: shipping-service
        image: rojas43529/shipping-service:dev-latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8600
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "dev"
        - name: SPRING_CLOUD_DISCOVERY_ENABLED
          value: "false"
        envFrom:
        - configMapRef:
            name: microservices-config
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8600
          initialDelaySeconds: 45
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8600
          initialDelaySeconds: 60
          periodSeconds: 10
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: ecommerce-dev
  labels:
    app: shipping-service
spec:
  selector:
    app: shipping-service
  ports:
  - port: 8600
    targetPort: 8600
    name: http
  type: ClusterIP
